import type { Config } from './config.js';
import type { Addon, GitHub, Import, OptionalMeta, RequiredMeta } from './types.js';
export declare const osSlash: string;
/**
 * Get the config with defaults if optional properties aren't passed.
 */
export declare const getConfig: () => Promise<{
    meta: RequiredMeta & OptionalMeta;
    root: string;
    main: string;
    betterdiscord: {
        themeFolder: string;
    };
    vencord: {
        themeFolder: string;
        dev: boolean;
    };
    addons: Addon[];
    github: GitHub;
    imports: Import[];
}>;
/**
 * Get the client mod datafolder.
 * @param mod The directory to search for.
 */
export declare const getDataFolder: (mod: "BetterDiscord" | "Vencord") => string;
/**
 * Transforms the given value to an absolute path.
 */
export declare const getPath: (...val: string[]) => string;
/**
 * Construct the meta given by the `bd-scss.config.js` file.
 */
export declare const generateMeta: (meta: Config["meta"]) => string;
/**
 * Splits any `@imports` into their own array and remove from string.
 * @param css The css to parse
 */
export declare const splitImports: (css: string) => {
    css: string;
    imports: string[];
};
/**
 * Parse each addon.
 * @param addon The singular addon config object
 */
export declare const parseAddon: (addon: Addon) => {
    target: string;
    name: string;
    dev: boolean | undefined;
    dist: boolean | undefined;
};
/**
 * Get the last index of the string and remove the any starting underscore.
 * @param str The string to be popped
 */
export declare const pop: (str: string) => string;
/**
 * Generate a CSS import based on the `bd-scss.config.js` file.
 * @param config The config object.
 * @param name The name of the file.
 */
export declare const generateImport: (config: Config, name: string) => string;
//# sourceMappingURL=utils.d.ts.map